pipeline {
    agent any
    
    stages {
        stage('test') {
            steps {
                 script {
                    mvn -B  -f ./test clean release:clean release:prepare release:perform -DlocalCheckout=true -DdryRun=false
                 }
                }
            }
        }
    }
