pipeline {
    agent any;    
    stages {
        stage('test') {
            steps {
                 script {
                    bash '''
                    #!/bin/bash
                    mvn -B  -f ./test clean release:clean release:prepare release:perform -DlocalCheckout=true -DdryRun=false -Dscm.user='RITESH_TOKEN' -Dscm.pass='RITESH_TOKEN'
                    '''
                 }
                }
            }
        }
    }